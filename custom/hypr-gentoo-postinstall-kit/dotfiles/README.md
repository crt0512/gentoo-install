# Dotfiles Payload

Put your reusable user configs here.

Typical structure:

- `dotfiles/hypr/`
- `dotfiles/waybar/`
- `dotfiles/wofi/`
- `dotfiles/mako/`
- `dotfiles/kitty/`

Quick way to populate from your current machine:

```bash
./scripts/00-export-current-config.sh
```
